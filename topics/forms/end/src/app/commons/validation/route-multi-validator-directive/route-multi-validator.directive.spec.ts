import { RouteMultiValidatorDirective } from './route-multi-validator.directive';

describe('RouteMultiValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteMultiValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
