import { CityAsyncValidationDirective } from './city-async-validation.directive';

describe('CityAsyncValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CityAsyncValidationDirective();
    expect(directive).toBeTruthy();
  });
});
